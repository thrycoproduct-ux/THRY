{
  "rules": [
    {
      "allowed": {
        "origins": [
          "http://localhost:3000",
          "http://127.0.0.1:3000"
        ],
        "methods": ["GET", "PUT", "HEAD"],
        "headers": ["Content-Type", "Authorization"]
      },
      "exposeHeaders": ["ETag"],
      "maxAgeSeconds": 86400
    }
  ]
}
